import React from "react";

const Loading = () => {
  return (
    <div className="h-screen flex justify-center items-center">
      <button class="btn loading btn-accent ">loading</button>
    </div>
  );
};

export default Loading;
